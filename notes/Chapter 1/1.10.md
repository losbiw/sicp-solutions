
```lisp
(define (A x y)
	(cond ((= y 0) 0)
		  ((= x 0) (* 2 y))
		  ((= y 1) 2)
		  (else (A (- x 1)
			  (A x (- y 1))))))
```

1. (A 1 10)

(A 1 10)
(A (- 1 1) (A 1 (- 10 1)))
(A 0 (A 1 9))
(A 0 (A (- 1 1) (A 1 (- 9 1)))))
(A 0 (A 0 (A 1 8)))
(A 0 (A 0 (A 0 (A 1 7))))
(A 0 (A 0 (A 0 (A 1 7))))
...
2 ^ 10 = 1024

2. (A 2 4)

(A 2 4)
(A (- 2 1) (A 2 (- 4 1)))
(A 1 (A 2 3))
(A 1 (A (- 2 1) (A 2 (- 3 1))))
(A 1 (A 1 (A 2 2)))
(A 1 (A 1 (A (- 2 1) (A 2 (- 2 1)))))
(A 1 (A 1 (A 1 (A 2 1))))
(A 1 (A 1 (A 1 2))
(A 1 (A 1 (A (- 1 1) (A 1 (- 2 1))))))
(A 1 (A 1 (A 0 (A 1 1))))
(A 1 (A 1 (A 0 2))
(A 1 (A 1 4))

```lisp
(define (f n) (A 0 n))
```
**f(n)** = 2n
```lisp
(define (g n) (A 1 n))
```

**(g n)** becomes:
(A 1 n)
(A (- 1 1) (A 1 (- n 1)))
(A 0 (A 1 (- n 1)))

**g(n)** = 2^n

```lisp
(define (h n) (A 2 n))
```
**(h n)** computes:
(A 2 n)
(A (- 2 1) (A 2 (- n 1)))
(A 1 (A 1 (A 1 1)))
(A 1 (A 1 2))
(A 1 (A 0 (A 1 1)))
(A 1 (A 0 2))
(A 1 4)
16

OR using formula:
```lisp
(define (A x y)
	(cond ((= y 0) 0)
		  ((= x 0) * 2 y)
		  ((= y 1) 2)
		  (else (A (- x 1) 
			  (A x (- y 1))))))
```

Simplifying:
```lisp
(define (A 2 n)
	(cond ((= n 0) 0)
		  ((= 2 0) * 2 y) // never true
		  ((= n 1) 2)
		  (else (A (- x 1) 
			  (A x (- y 1))))))
```

```lisp
(define (A x y)
	(cond ((= y 0) 0)
		  ((= y 1) 2)
		  (else (A 1 (A 2 (- n 1))))))
```

Define in terms of g(n)
(A 2 n) = h(n), therefore h(n - 1) = (A 2 (- n 1))

Hence, $h(n) = 2^{h(n - 1)} = 2^{2^{h(n - 2)}} = {^{n}2}$ (tetration of 2)
