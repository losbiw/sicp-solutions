```lisp
(define (+ a b)
	if (= a 0)
	b
	(+ (dec a) (inc b))))
```

(+ 4 5) expansion:
(+ 4 5)
(+ (dec 4) (inc 5))
(+ (dec 3) (inc 6))
(+ (dec 2) (increase 7))
(+ (dec 1) (increase 7))
(+ 0 8)
8

Recursive procedure, iterative process