The `sqrt-stream` procedure itself isn't memoized and every recursive call to it creates a new sequence, thus rendering the memo-proc optimization useless. Thus, this version behaves the same as if there was no memo-proc.