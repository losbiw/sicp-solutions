A: (lambda () (set! x (*B*)))
B: (s (lambda () (* x x)))
C: (s (lambda () (set! x (+ x 1))))

Possibilities:
B -> A -> C (101): A+B set x to x^2 (100), C increments it by 1
B -> C -> A (100): B computes result to be 100, C increments x to 11, A sets x to 100
C -> B -> A (121): C increments x by 1, A+B set x to x^2 (121)
