
The overall implementation would remain largely the same as in exercise 3.25. However, the assoc procedure would be such that would support binary-tree representations of the records list (i.e similar to 2.66)

Insertion procedure would have to treat the list of records as a self-balancing binary-tree, so as to optimize retrieval.

The form:
('\*table\*, ((key . value) . left . right))
or
('\*table\*, nil)