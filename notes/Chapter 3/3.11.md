The state is stored in the first frame of the environment E1, which is created during a call to make-account which binds the formal parameter balance. Symbols for withdraw, deposit and dispatch are bound to the respective procedure objects created within E1 (not shown clearly in diagram).

![[3.11-sicp.png]]

Some code might be shared between acc and acc2, however that could only potentially be the definition for the dispatch procedure as it does not refer to the state in its enclosing environment. However, that is implementation-specific.