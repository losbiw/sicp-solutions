Initial output: 0 (at 1,0)
Should end up at: 0 (at 0,1)

After setting 1st input to 1, the segment queue contains delayed action to set output based on inputs (1,1)

After settings 2nd input to 0, the segment queue contains delayed action to set output based on inputs (1,0)

In queue version (FIFO), the output is first set to 1 and then to 0. In stack version, output is first set to 0 and then incorrectly ends up at 1.