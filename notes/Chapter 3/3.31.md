without immediately executing the action procedure, the lambda functions specified inside after-delay will not be added to added to the agenda. Thus, once propagate is called the first time, inputs end up in an invalid state without manually changing state first to trigger the event callbacks.