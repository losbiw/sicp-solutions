![[3.46.png]]

Note: mutex is a cell (one-item list) because that way you can pass it to other functions by reference (i.e. `test-and-set!`), as opposed to a boolean variable which would be passed by value.