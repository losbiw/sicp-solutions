Unserialized balance will always reflect the latest changes applied to the balance variable, regardless of what it is, thus giving the "correct" answer for that point in time. There is no interleaving that can happen since this is a one-step operation. On the contrary, serializing the balance procedure would result in less-than-ideal behavior where a user cannot see their balance if a withdrawal or a deposit is happening at the same time.