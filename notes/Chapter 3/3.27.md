![[3.27.png]]

The memo-fib is $\theta(n)$ because the fibonnaci value has to be computed only once for each value. I.e once fib(5) is computed, fib(6) simply sums the pre-calculated values of fib(5) and fib(4), thus adding one extra step for each new value. 

No because internally memo-fib still recursively calls the non-memoized version of fib, thus bypassing the memoization mechanism.
