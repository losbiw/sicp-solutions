If an exchange between account-1 and account-n is facilitated by a number of accounts x. such that 1 < x < n, but it is not known which locks need to be acquried for the transfer beforehand. Locks are acquired for a1 and an, and then and only then can they be acquired for the intermediary accounts.

However, then if another exchange with intermediaries is instatiated at the same time between some 2 accounts that are acting as intermediaries in the first transaction and have a1 and an as its intermediaries, the overall structure would end up in a deadlock (similar to Paul and Peter above)