Trees in Fig. 2.16: A, B, C (left-to-right)

a. Procedures produce identical results for any tree.

b. First procedure uses `append` procedure, which performs n steps. Thus, `tree->list1` performs n steps at each level with a total of log(n) levels. $\Theta(n\log n)$

Second procedure performs constant work at each level (cons), thus it is $\Theta (\log n)$
